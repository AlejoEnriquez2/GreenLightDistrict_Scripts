- Check that the virtual microphone and virtual camera loops properly (X)
- Video of virtual camera is not looping (X)
- Script.py is again inside the skype folder (X)
- Finish configuring slack clicks (X)
- Clean files and paths (X)
- Fix the virtual microphone, everytime opens an app needs to state to use the virtual one (X)
- Make the virtual microphone have the same content for all the iterations (X)
    - Maybe just send a frequency audio loop (X)
    - Make the audio start at every iteration (X)
- Add sound microphone app clicks after every *startMeeting.csv
    - 1579,128
    - 1794,100